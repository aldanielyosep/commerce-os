# TDD-000 architecture (Legacy)

Status dokumen ini: legacy reference only.

Untuk baseline arsitektur aktif, gunakan dokumen berikut:

- [ARCH-000 Foundation Architecture](../../arch/ARCH-000-foundation.md)

Alasan perubahan:

- Dokumen ini berisi konteks lama yang sudah tidak mencerminkan scope aktif 3 sistem (admin-web, api, storefront).
- Baseline teknis terbaru (auth mandatory, paging/search/order default, quality and security gates, storage dan jobs) telah dipindahkan ke ARCH-000.

Dokumen ini dipertahankan untuk histori keputusan lama, bukan sebagai source of truth implementasi saat ini.
